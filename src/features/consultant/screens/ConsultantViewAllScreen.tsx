import { View, Text, StyleSheet } from 'react-native';

export default function ConsultantViewAllScreen() {
  return (
    <View style={styles.container}>
      <Text>Consultant View All Screen</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

