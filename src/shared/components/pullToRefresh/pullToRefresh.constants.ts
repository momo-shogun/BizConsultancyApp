/** Distance (px) the user must pull before refresh arms. */
export const PTR_THRESHOLD = 72;

/** Max visual travel for the indicator while pulling. */
export const PTR_MAX_PULL = 112;

/** Resting Y offset of the indicator while a refresh is in flight. */
export const PTR_REFRESHING_OFFSET = 14;

/** Indicator diameter. */
export const PTR_SIZE = 36;

/** Stroke width of the progress / spin arc. */
export const PTR_STROKE = 2.5;
